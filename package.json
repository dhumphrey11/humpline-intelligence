{
  "name": "humpline-crypto",
  "private": true,
  "workspaces": [
    "services/*",
    "shared",
    "web"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "lint": "npm run lint -ws",
    "test": "npm run test -ws"
  }
}
